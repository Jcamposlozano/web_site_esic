{
  "name": "esic-react-app",
  "version": "0.0.1",
  "private": true,
  "description": "Monorepo for ESIC Medellin website migration to Astro + React + Payload CMS",
  "scripts": {
    "dev": "pnpm --filter web dev",
    "build": "pnpm --filter web build",
    "preview": "pnpm --filter web preview"
  },
  "engines": {
    "node": ">=20",
    "pnpm": ">=9"
  },
  "packageManager": "pnpm@11.3.0"
}
