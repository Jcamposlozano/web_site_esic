node_modules
.next
/build
/dist

# DB local (SQLite) — no se commitea, se regenera con el seed
cms.db
cms.db-shm
cms.db-wal
*.db
*.db-journal

# Media subida en local — no se commitea (las portadas seed vienen de apps/web)
/media/*
!/media/.gitkeep

# Tipos generados
payload-types.ts

# env
.env
.env.*
!.env.example

next-env.d.ts
