/**
 * Ícono compacto de ESIC para la barra de navegación del admin.
 * Badge azul con "E". Estilos en custom.scss.
 */
export function Icon() {
  return <span className="esic-icon" aria-label="ESIC">E</span>
}

export default Icon
